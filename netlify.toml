[build]
  # Pas de commande de build = build instantané
  command = ""
  publish = "."
  
[functions]
  directory = "netlify/functions"

# Désactiver les builds automatiques pour les branches
[context.branch-deploy]
  command = ""
